{
  "name": "vemu-email-automachine",
  "version": "1.0.0",
  "private": true,
  "description": "Email Auto Machine - MERN app that sends personalized emails from spreadsheets",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm start --prefix server\" \"npm run dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
